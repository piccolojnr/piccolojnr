---
import "../styles/global.css";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import {
  SITE_NAME,
  PERSON_NAME,
  TWITTER_HANDLE,
  DEFAULT_OG_PATH,
  DEFAULT_OG_IMAGE_ALT,
} from "../lib/site";

interface Props {
  title: string;
  description: string;
  canonicalPath: string;
  ogImage?: string;
  /** Alt text for og:image / Twitter image (accessibility + some platforms). */
  ogImageAlt?: string;
  ogType?: string;
  keywords?: string;
  jsonLd?: object;
  noIndex?: boolean;
}

const {
  title,
  description,
  canonicalPath,
  ogImage,
  ogImageAlt,
  ogType = "website",
  keywords,
  jsonLd,
  noIndex = false,
} = Astro.props;

const site = Astro.site ?? new URL("http://localhost:4321");

const canonicalURL = new URL(canonicalPath, site).href;
const imagePath = ogImage?.startsWith("http")
  ? ogImage
  : ogImage || DEFAULT_OG_PATH;
const ogImageURL = imagePath.startsWith("http")
  ? imagePath
  : new URL(imagePath.startsWith("/") ? imagePath : `/${imagePath}`, site).href;

const ogImageAltResolved = ogImageAlt ?? DEFAULT_OG_IMAGE_ALT;

---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,500;0,9..144,600;0,9..144,700;1,9..144,400&family=Source+Sans+3:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap"
      rel="stylesheet"
    />
    <link rel="icon" href="/favicon.ico" sizes="any" type="image/x-icon" />
    <link rel="apple-touch-icon" href="/favicon.ico" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    {keywords ? <meta name="keywords" content={keywords} /> : null}
    <meta name="theme-color" content="#faf8f4" />
    <link rel="canonical" href={canonicalURL} />
    {
      noIndex ? (
        <meta name="robots" content="noindex, nofollow" />
      ) : null
    }

    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:type" content={ogType} />
    <meta property="og:locale" content="en_GH" />
    <meta property="og:site_name" content={SITE_NAME} />
    <meta property="og:image" content={ogImageURL} />
    <meta property="og:image:alt" content={ogImageAltResolved} />
    {
      ogType === "article" ? (
        <>
          <meta property="article:author" content={PERSON_NAME} />
          <meta property="article:section" content="Case study" />
        </>
      ) : null
    }

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content={TWITTER_HANDLE} />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageURL} />
    <meta name="twitter:image:alt" content={ogImageAltResolved} />

    {
      jsonLd ? (
        <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
      ) : null
    }
  </head>
  <body class="min-h-screen bg-background flex flex-col">
    <Navbar />
    <main class="flex-1 pt-14 md:pt-16 w-full">
      <slot />
    </main>
    <Footer />
  </body>
</html>
